version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: btech-api
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - ENV=development
    restart: unless-stopped
